apiVersion: v1
kind: Pod
metadata:
  name: pod5
  labels:
    app: myApp
spec:
  containers:
  - name: c1
    image: httpd
    ports:
    - containerPort: 80
      name: httpd-port
      protocol: TCP
