apiVersion: v1
kind: Pod
metadata:
  name: pod1
  labels:
    app: myApp
spec:
  containers:
  - name: c1
    image: httpd
    ports:
    - containerPort: 80
      name: http-port
      protocol: TCP
